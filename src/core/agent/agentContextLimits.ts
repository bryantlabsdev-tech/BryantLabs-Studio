/** Max characters returned from agent read_file previews (raised from 2k for real edits). */
export const AGENT_FILE_PREVIEW_CHARS = 12_000;

/** Max files pre-read before edit planning (Phase 3: raised to 30). */
export const DEFAULT_MAX_EXPLORE_FILES = 30;
